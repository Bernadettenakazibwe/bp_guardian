# BP Guardian Backend

Simple Flask + SQLite backend for BP Guardian.

## Quick start

1. Create virtual environment and activate it
2. Install dependencies:

   pip install -r requirements.txt

3. Run the app:

   python run.py

4. Open in browser:

   http://127.0.0.1:5000/ping
